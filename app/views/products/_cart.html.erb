<% if !@cart.nil? and !@cart.empty? %>
	<table>
		<% @cart.items.each do |item| %>
			<tr>
				<td class="cart_name"><%= truncate(item.name, 15) %></td>
				<td class="cart_qty"><%= item.quantity %></td>
				<td class="cart_prc"><%= number_to_currency item.price %></td>
			</tr>
		<% end %>
		<tr>
			<td colspan="2">Total</td>
			<td><strong><%= number_to_currency @cart.price %></strong></td>
		</tr>
	</table>
	<p><%= link_to "Checkout", @cart %></p>
<% else %>
	<p style="margin:0px;padding:.5em;">
		You have no items on your cart.
	</p>
<% end %>