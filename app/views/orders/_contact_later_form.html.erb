<p>If you want us to contact you personally to complete your transaction.</p>

<p>Please click on the following button.</p>

<% form_for order, :url => orders_path() do |f| %>
	<%= error_messages_for :contact_later_order %>
	<%= f.hidden_field :payment_method %>
	<%= f.hidden_field :order_type %>
	<%= f.submit "Contact Me" %>
<% end %>