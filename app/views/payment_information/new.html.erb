<% title "Winnamoney | Registro" %>

<h1>Suscription information</h1>
<table style="text-align:left;border:1px solid black;">
	<tr>
		<th>Store name</th>
		<th>Suscription time</th>
		<th>Amount</th>
	</tr>
	<tr>
		<td>http://<%= @user.store.name %>.winnamoney.com</th>
		<td>1 year</th>
		<td>189.00 US$</th>
	</tr>
</table>

<div class="tabs">
  <!-- tabs -->
<br />
  <strong>Payment type:</strong>
  <ul class="tabNavigation" >
    <li><a href="#paypal">Paypal</a></li>
    <li><a href="#deposit">Bank deposit</a></li>
	<li><a href="#other">Otro</a></li>
  </ul>

  <!-- tab containers -->
  <div id="paypal">
    <%= render :partial => 'sandbox_subscription' %>
  </div>
  <div id="deposit">
    <%= render :partial => 'form', :locals => {:payment_information => @payment_information, :user => @user } %>
  </div>
  <div id="other">
   	<p>Un representante de Winnamoney puede ponerse en contacto contigo para apoyarte a finalizar tu compra.</p>
	<p>Si as&iacute; lo deseas por favor haz click en el siguiente bot&oacute;n:</p>
	<% form_for @payment_information, :url => user_payment_information_path(@user) do |f| %>
		<%= f.hidden_field :contact_me %>
		<%= f.submit "Finalizar Registro" %>
	<% end %>
  </div>
</div>

<script type="text/javascript" charset="utf-8">
	$(function () {
	    var tabContainers = $('div.tabs > div');

	    $('div.tabs ul.tabNavigation a').click(function () {
	        tabContainers.hide().filter(this.hash).show();

	        $('div.tabs ul.tabNavigation a').removeClass('selected');
	        $(this).addClass('selected');

	        return false;
	    }).filter(':first').click();
	});
</script>